@AGENTS.md

# Carte des documents du repo

- `docs/PLAN.md`, `docs/HANDOFF_CC_LOCAL.md`, `docs/AUDIT_CONFORMITE.md` : l'ATELIER (plan, reprise, conformite maquette-code ecran par ecran).
- `handoff/` : handoff maquette Atelier (reference, jamais edite).
- `docs/handoff-site/` : handoff du SITE PUBLIC (relais, INTEGRATION.md V2, maquettes desktop/mobile autonomes, plan d'integration — jamais edites).
- `docs/site/ARCHITECTURE.md` : architecture du site public (monorepo, acces donnees service_role, plan de migration referentiel, risques Vagues 2-3).
- `docs/site/CONFORMITE_VAGUE_1.md` : conformite maquette-code du site, Vague 1.
- `site/` : app Next.js du site public (port 3002, README env : `site/.env.local.example`).

# Etat du chantier site public (a tenir a jour a chaque fin de session)

- **VAGUE 4 EN PRODUCTION depuis le 2026-07-20** (merge `--no-ff` `0223acd` « Merge Vague 4 » ; les DEUX projets Vercel rebuildes ; **test prod E2E par Arnaud OK** : compte client reel sur aleonmange.app, parcours complet inscription -> mail -> rattachement -> fidelite -> re-commande -> profil, visuels conformes). Espace client authentifie (`/compte`) + programme fidelite (compteur DERIVE, jamais stocke) + fiche client Atelier ; migrations 0034-0040 deja appliquees en prod (le deploiement ne touche pas la base) ; garde Atelier `app_role=equipe` (Fix C, deja hotfixe `1ba235a`, RECONCILIE SANS doublon au merge : diff Fix C vide). Vagues 1-2-3 EN PRODUCTION sur `main` depuis le 2026-07-19. Detail + preuves : `docs/site/CONFORMITE_VAGUE_4.md`.
- VAGUE 1 (vitrine lecture) EN PRODUCTION sur **https://aleonmange.app** depuis le 2026-07-19 (www → apex 308 ; Atelier sur **https://atelier.aleonmange.app**) ; role `site_lecteur` (JWT echeance 2036-07-18). Plan de migration referentiel 0019-0023 APPLIQUE le 2026-07-18. Etat live, rollback, pieges Vercel : `docs/site/ARCHITECTURE.md`. Conformite : `docs/site/CONFORMITE_VAGUE_1.md`.
- VAGUES 2 ET 3 EN PRODUCTION depuis le 2026-07-19 (MERGE `--no-ff` `a48b497` « Merge Vagues 2 et 3 », les DEUX projets Vercel rebuildes). Le site prend des commandes (click&collect boutique + truck, devis traiteur, newsletter double opt-in) et le chef les confirme/refuse et les traite dans l'Atelier.
  - VAGUE 2 (les trois ecritures) : migrations 0024-0030. Ecritures via role `site_ecrivain` (JWT echeance 2036-07-19 ; AUCUN droit table, EXECUTE sur 4 RPC SECURITY DEFINER only) ; prix recalcule en SQL ; create-or-match (email prioritaire, telephone secours) ; emails Resend best-effort (mode dev). Details : `docs/site/CONFORMITE_VAGUE_2.md`, mecanisme : ARCHITECTURE.md section Vague 2.
  - VAGUE 3 (confirmation chef) : migrations 0031-0032. Confirmation (depliage bowl via source unique `composerLignesComposantBowl`), refus (marqueur, REGLE PERMANENTE : tout lecteur de `web_a_confirmer` filtre `refuse_le`), remise B2C (moyen corrige au retrait), badge + section /orders + notification (trigger defensif), editeur `creneau_retrait`, emails Atelier (module propre). Details : `docs/site/CONFORMITE_VAGUE_3.md` + ARCHITECTURE.md section Vague 3.
- GO-LIVE TESTE DE BOUT EN BOUT EN PRODUCTION le 2026-07-19 : 2 vraies commandes truck sur aleonmange.app → notification (trigger) + pastille + section chef → CONFIRMATION chef (depliage bowl : composants conformes recette, entree dans `v_commande_ouverte`) → REMISE chef (moyen especes, `reglement` + `encaisse_le` poses, entree en CA facture ET encaisse). Cycle complet prouve en base. Donnees d'essai NETTOYEES (comptages a zero verifies : 0 vente web, 0 client test, 0 notification `commande`, 0 orphelin).
- HOTFIXES SITE LIVRES le 2026-07-19 (branche `hotfix-precommande`, merge `--no-ff`, EN PRODUCTION sur le site ; seul le projet SITE a rebuilde, l'Atelier a saute = nouvelle preuve de l'Ignored Build Step) :
  1. `/food-truck/precommander` — date de retrait COMPLETE (jour + date, ex. « Mardi 21 juillet ») cote client dans le selecteur d'emplacement ET un recap, via `prochainRetraitTruck` (`site/src/lib/data/creneaux.ts`) cable dans `precommander/page.tsx` : MEME source que le `due_at` pose a la commande (`/api/commande`) -> affichage coherent avec le calcul Europe/Paris. Les emails portaient DEJA la date complete (site « precommande recue » = `retraitLabel` ; Atelier « confirmee » = `labelRetrait`).
  2. `BoutonSubmit` (`site/src/components/forms.tsx`) : `className` passee est desormais destructuree hors des props et FUSIONNEE dans les classes de base, au lieu d'etre ecrasee par le spread `{...props}` -> le CTA « Envoyer ma demande » retrouve le style pilule accent (boutique + truck). E2E : classe fusionnee verifiee dans le HTML SSR des deux parcours, commande d'essai truck -> retrait affiche « Mardi 21 juillet » == `due_at` en base (2026-07-21 11:30 Europe/Paris, isodow 2), builds verts des deux apps, donnees d'essai nettoyees.
- IMAGES SITE (visuels du handoff CD) EN PRODUCTION depuis le 2026-07-19 (merge `--no-ff` `3cb18b2` ; seul le projet SITE a rebuilde, l'Atelier a SAUTE = Ignored Build Step, prouve : deploiement Atelier CANCELED). Verif prod : images rendues sur les 5 pages, portraits chefs en placeholder, poids webp 79-165 Ko (next/image sert ~67 Ko). **AMENDEMENT REGLE (decision Arnaud) :** la regle « aucune image generee par IA » visait les FAUSSES representations non validees, PAS les visuels du handoff CD (direction artistique validee) ; ces visuels sont integres et **restent remplacables par un shooting reel plus tard SANS changement de code** (memes emplacements, memes noms de fichiers). Controle anti-Foodizy MAINTENU (RAS sur les 22 visuels extraits des maquettes). Detail : `docs/site/ARCHITECTURE.md` section Visuels.
  - Ambiance -> composant `Photo` (next/image, webp `site/public/images/`, alt FR sans cadratin, priority sur les hero) : accueil (hero « la maison » + 3 cartes canaux), boutique (comptoir), food-truck (le truck + au marche), traiteur (hero + galerie 4 receptions), histoire (laboratoire).
  - **PORTRAITS DES CHEFS** (« Audrey et Victorien » : accueil + histoire) : `PhotoAvenir` MAINTENU, decision definitive de cette vague (jamais de visage genere presente comme un vrai chef ; ce sera la 1re photo du shooting reel).
  - Plats par la DONNEE : migration **0033** `produit.image_url` (additif, faible risque, applique + verifie). 3 produits boutique pourvus (Parmentier de boeuf, Cordon bleu, Lasagnes bolognaise) -> section « Nos recettes signatures » pilotee par le catalogue (fallback etat vide propre si aucune image).
  - Ecartes : logo fond noir (detourage sale), image « plan d'acces » (figee, mail @yahoo d'artefact) -> a la place le lien « Voir le plan d'acces » ouvre Google Maps ITINERAIRE sur l'adresse reelle (`COORDONNEES.plan`).
  - EN ATTENTE (a trancher/associer, listes au chef) : **bowl + tartines** (signatures truck) et **blanquette + paella** (aucun produit correspondant au catalogue) non poses ; upload d'images produit par les chefs dans l'Atelier (Vague 4) ; demander a CD un **logo vectoriel fond transparent** pour l'en-tete.
- VAGUE 4 — **EN PRODUCTION (merge `0223acd`, 2026-07-20)**. Socle DB + infra auth + Phase B, sur `main` (migrations **0034-0040 APPLIQUEES EN PROD** ; hook Auth ACTIVE ; etat final verifie le 2026-07-19 : trigger `handle_new_user` FAIL-CLOSED + signups ON). Inclut le correctif securite **0040** (faille self-provision chef fermee) et le write-path client **0039** (RPC rattachement email verifie + opt-in + maj profil). **MERGEE EN PROD (`0223acd`, `--no-ff`, les 2 projets Vercel rebuildes)** ; **main = Vagues 1-4**. Plan : `~/.claude/plans/delegated-tumbling-pearl.md`. Detail + preuves : `docs/site/CONFORMITE_VAGUE_4.md` ; modele de securite : `docs/site/ARCHITECTURE.md` section Vague 4.
  - Migrations : 0034 (hook claim `app_role` + `est_chef()` fail-closed ; son trigger `handle_new_user` **remplace par 0040**), 0035 (bascule 60 policies `using(true)` -> `using(est_chef())` + 5 scoped durcies), 0036 (isolation client : `client.auth_user_id`, `mon_client_id()`, policies client SELECT, `client_preference`), 0037 (fidelite : opt-in, `parametre_fidelite`, `fidelite_redemption`, vue derivee `v_fidelite_client`), 0038 (create-or-match email prioritaire strict), **0039** (write-path client : RPC `web_rattacher_compte_client` [email VERIFIE, param-free] + `web_maj_profil_client` [opt-in date + profil], SECURITY DEFINER self-scope, EXECUTE authenticated only, garde est_chef), **0040** (`handle_new_user` FAIL-CLOSED : profil pour le bootstrap owner uniquement ; ferme la faille self-provision chef exposee par l'activation des signups).
  - **SOCLE FAIT (Etage 2a)** : infra auth `@supabase/ssr` server-only (aucun nouveau secret, jamais NEXT_PUBLIC), `proxy.ts` cadre STRICTEMENT `/compte` (vitrine publique intacte), Server Actions inscription `kind='client'` / connexion / deconnexion, callback confirmation (PKCE `?code=` + `verifyOtp ?token_hash=`) -> rattachement 0039 + opt-in, ecrans SOCLE sobres (connexion + /compte) ; Atelier `/login` action `signUp` supprimee. **Build + lint verts des 2 apps.** **E2E socle A VALIDER par Arnaud** (inscription -> mail -> lien -> rattachement -> isolation) AVANT Phase B.
  - **PHASE B FAITE** (buildee + prouvee, non mergee) : (a) 4 ecrans maquette CD (d-login, d-compte carte fidelite tampons derives + chips + statut verrouille, d-cmd + re-commande 1-geste [recompose le panier -> `/api/commande` web_a_confirmer, create-or-match email], d-profil coordonnees/consentements/preferences) + entree « Mon compte » en-tete ; (b) Atelier fiche client `/clients` : palier `v_fidelite_client` + geste « appliquer recompense » (`fidelite_redemption`, chef) ; (c) E2E data-path prouve (compteur derive, re-commande attachee/chef-gated, opt-in non retroactif, isolation) + **tableau de conformite ecran par ecran** dans `CONFORMITE_VAGUE_4.md`. Ecarts volontaires signales (CP lecture seule, newsletter = action S'abonner, suppression compte = demande email, zero cadratin).
  - **Constats de la validation socle (Arnaud)** : socle valide (inscription -> mail -> lien -> rattachement -> isolation OK). Deux correctifs livres : **Fix B** (site_url -> aleonmange.app + emailRedirectTo exact : les confirmations client ne partent plus vers l'Atelier) ; **Fix C / constat C** (garde Atelier exigeait seulement « connecte », pas le claim -> un compte client atteignait l'UI Atelier ; corrige : middleware + signIn + layout exigent `app_role=equipe`). Fix C **HOTFIXE en prod (main, `1ba235a`)** ; Fix B sur site-vague-4.
  - **GO-LIVE FAIT (2026-07-20)** : validation visuelle 390/1440 OK ; merge `--no-ff` `0223acd` ; 2 deploiements Vercel READY (Atelier `atelier.aleonmange.app` build ~44 s ; site `aleonmange.app` : nouveau logo HD + email yahoo servis en prod, verifies) ; test prod E2E Arnaud OK. Config Auth posee (site_url `aleonmange.app` + redirect URLs `/compte/**` dev+prod). Donnees de demo conservees (nettoyage = backlog PRE-OUVERTURE).
  - Backlog technique :
  - ⭐ **PRE-OUVERTURE (a solder avant l'ouverture reelle au public)** : (1) **mentions legales** = contenu juridique reel (raison sociale, SIRET, responsable de publication, clause RGPD revocable ; page aujourd'hui en placeholders, `robots noindex`) ; (2) **nettoyage cible** des **5 precommandes en retard du 13-18 juillet** + eventuelles donnees d'essai (par id/email, JAMAIS de sweep ; la demo reste INTACTE jusque-la) ; (3) **re-bascule de l'email affiche** quand le camion changera d'adresse (source unique `COORDONNEES.email` ; resynchroniser AUSSI `site/public/llms.txt`, copie statique) ; (4) **domaine Resend `aleonmange.app`** : **FAIT le 2026-07-21** (verifie DNS + `RESEND_PROD=1` pose sur les 2 projets Vercel -> emails transactionnels en PROD).
  - **/users : REPORTE au backlog** (pas de recrutement prevu ; seuls Audrey et Victorien rejoignent). **Provisioning MANUEL** (chemin legitime 0040, fail-closed) : invitation Supabase (Auth > Users > Send invitation) -> compte auth SANS profil -> insertion explicite `profil` (id=uid, nom, role) -> claim `app_role=equipe` au 1er jeton. **FAIT le 2026-07-20** : compte d'equipe **PARTAGE** `aleonmange@yahoo.com` (uid `4aaef25d`), nom d'affichage « Audrey et Victorien », role `owner` (comptes individuels possibles plus tard). Pas-a-pas + piege `site_url` : `docs/site/ARCHITECTURE.md` section Vague 4. Ecran d'invitation `/users` = backlog (bouton desactive assume).
  - **Atelier `/login`** : **FAIT (2026-07-19, commit `9d79503`)** — action `signUp` non cablee supprimee (elle aurait provisionne un chef, cf. faille 0040) + commentaires des 2 fichiers corriges. L'UI n'invitait deja pas a creer un compte.
  - **Refonte responsive mobile de l'Atelier : EN PRODUCTION** (merge `--no-ff` `6346861`, 2026-07-20 ; **seul l'Atelier a rebuilde, le site a saute** = 0 fichier `site/` touche ; validation 390 par Arnaud au telephone + 1440 inchange). Motif : les chefs se connectent au telephone (ecran inutilisable avant). Shell repliable (drawer + hamburger + overlay < 1024px) + les ~20 ecrans a 390px : **cartes** sur catalogue & stocks (les 2 tableaux chefs quotidiens), **scroll horizontal** (`fz-tscroll`) sur les autres faux-tableaux, **drawers plein ecran**, formulaires en 1 colonne ; 2 polish (drawer produit : action destructive isolee ; badge famille de carte « Aucun produit » explicite). Idiome : classes + `@media` dans `src/app/globals.css` (l'inline ne porte pas de @media ; `!important` bat l'inline), **desktop 1440 INCHANGE**. Attestation ecran par ecran : `~/.claude/plans/linked-squishing-twilight.md`. Ecarts mineurs restants (non bloquants) : stocks « A racheter » cellules-inputs denses ; tableau des lots imbriques non scrollable (valeurs courtes).
  - **Import du catalogue chefs (JSON V2) : EN PRODUCTION** (merge `--no-ff` `4970b8d`, 2026-07-20 ; l'Atelier a rebuilde sans changement de comportement, **le site a saute** = 0 fichier `site/` ni `src/` applicatif ; Atelier deploiement READY, site skip verifie). Les chefs ont fourni 85 produits -> **84** (dedup « Salade de fruits ») + 13 familles inseres en **BROUILLONS INVISIBLES** (`visible_site=false`, `actif=true`) dans `produit`/`famille_carte` EXISTANTS, a completer par les chefs (prix boutique, photos, descriptions, verif allergenes). **DB DEJA EN PROD** (comme Vague 4 : la base precede le merge du code) : migrations **0041** (colonnes `tags`/`import_ref`/`allergenes`/`allergenes_verifies` + contrainte `produit_visible_requiert_prix`) et **0042** (relache `prix_selon_mode` : le mode interdit la colonne de prix de l'autre mode, autorise la sienne a NULL pour les brouillons) APPLIQUEES + prouvees ; import applique (idempotent insert-only ; 100 visibles demo INCHANGES ; 1 collision de nom signalee non ecrasee : « Tapenade d'olives noires »). Traiteur : prix importes. Boutique : sans prix donc invisibles (contrainte). **Allergenes importes NON verifies** (`allergenes_verifies=false`) : REGLE = le site ne les affiche jamais tant que non verifies (ARCHITECTURE.md). Script rejouable `scripts/import-catalogue-chefs.mjs` ; trace brute `docs/imports/a-leon-mange-catalogue-v2.json` ; feuille de route chefs `docs/imports/RAPPORT_SAISIE_CHEFS.md` (prix suggeres marche = document UNIQUEMENT, jamais en base). **Reste : session d'accompagnement chefs** (saisie prix boutique, photos, descriptions, verif allergenes produit par produit dans l'Atelier).
  - **SEO / GEO / tracking / accessibilite du site : EN PRODUCTION** (merge `--no-ff` `060c656` + re-trigger `efca50d`, 2026-07-20 ; le webhook Vercel du merge avait rate -> re-trigger par une touche `site/` [cf. memoire] ; live verifie : robots/sitemap/llms 200, JSON-LD FoodEstablishment servi avec geo reel + 9 openingHoursSpecification, canonical + og:url sur les 6 pages). ZERO changement visuel (metadata/scripts/attributs seulement). Build + lint verts, tags + JSON-LD verifies sur le HTML prerendu. (a) **Tracking** : `@vercel/analytics` + `@vercel/speed-insights` (cookieless, aucune banniere) montes au layout (endpoints `/_vercel/insights` + `/_vercel/speed-insights` servis 200 en prod ; Analytics ACTIF cote aleonmange-site, donnees visibles) -> reste a confirmer l'activation Speed Insights au dashboard aleonmange-site. (b) **SEO** : `metadataBase` + helper `buildMetadata` (`site/src/lib/seo.ts`, evite le non-deep-merge OG/canonical de Next 16) -> canonical + OpenGraph (og:url) + Twitter sur les 6 pages publiques ; 2 titres trimes <=60 rendu (accueil 52, traiteur 57) ; `robots:{index:false,follow:false}` sur `/compte/**` (ils etaient INDEXABLES) ; `sitemap.ts` (6 pages, exclut /compte + tunnels + mentions-legales) + `robots.ts` (disallow /compte, ref sitemap). **JSON-LD FoodEstablishment** alimente par la BASE : COORDONNEES + horaires machine via nouveau `horairesBoutiqueSpec()` (table `horaire_boutique`) + geo REEL fourni par Arnaud (45.9633, 4.5130) + `priceRange €€` + `addressCountry FR` ; `legalName`/`taxID` OMIS (rejoindront avec les mentions legales, backlog). (c) **GEO** : `site/public/llms.txt` (convention llmstxt.org). (d) **Accessibilite** : `aria-label` sur les champs sans nom (newsletter, connexion, re-commande) + `aria-hidden` sur les svg decoratifs (`Photo` imposait deja un alt, boutons-icone deja labellises). Tracking back-office **Atelier** : ajoute via la PR Vercel #1 (`@vercel/analytics` + `<Analytics/>` au layout racine), mergee le 2026-07-20 -> usage reel des chefs.
  - **Refonte /histoire + footer GLOBAL du site : EN PRODUCTION** (merge `--no-ff` `2fcb1f7`, 2026-07-21 ; webhook Vercel OK du 1er coup, 2 deploiements success ; live verifie : /histoire nouveau contenu + SEO intact, footer global partout, favicon servi ; validation visuelle 390/1440 Arnaud OK). D'apres la maquette CD « Notre Histoire » (`docs/handoffs/`). Page `/histoire` refaite (8 sections : hero `chefs-truck.webp` + badge, manifeste canard + mark de Leon, recit labo, les 3 C [SVG inline], parcours 01-04 [4e inversee ; mobile VERTICAL 1 col, choix Arnaud], pont canaux -> `/boutique` `/traiteur` `/food-truck`) ET **footer GLOBAL remplace sur toutes les pages** (bande beige `bg-bande` #EFE6D2, logo complet, bandeau marque + 4 colonnes [pitch+reseaux SVG / adresse+plan / horaires base / contact] + barre legale « BEAUJOLAIS · FAIT MAISON »). Config-source (footer lit COORDONNEES + prop horaires base), CTA « Restez informe » sur la modale existante (`ouvrirLettre`), plan d'acces = `COORDONNEES.plan`. **Header inchange** (etat actif deja gere). Favicon rafraichi (`favicon.ico` 16/32/48 depuis `icon.png` 338, mark de Leon ; solde le backlog « favicon 16px »). **ZERO regression SEO** (metadata /histoire + JSON-LD + canonical/og verifies sur le prerendu). Tokens net-new : `--color-bande` #EFE6D2 + `--color-encre` #3A4A44 (tout le reste = tokens existants). Ecarts assumes (valides Arnaud) : route `/food-truck` (vs /truck maquette), bande footer #EFE6D2 (header inchange), `getFullYear()` (vs © 2026), tiret cadratin du recit -> deux-points (regle zero cadratin). Assets REUTILISES (aucune extraction ; chefs = `chefs-truck.webp` neutre, jamais `chefs-portrait.png` Foodizy qui n'existe pas au repo). Build + lint verts. Branche `site-histoire-footer` supprimee apres merge ; favicon conserve tel quel (mark de Leon, jugement a l'usage).
  - **GA4 + banniere de consentement CNIL : EN PRODUCTION** (merge `--no-ff` `d87ec28`, 2026-07-21 ; webhook OK, 2 deploiements success ; live verifie : « Gerer les cookies » global, defaut Consent Mode denied present, AUCUN gtag.js avant consentement, SEO intact, Vercel Analytics coexiste). Google Analytics 4 (`NEXT_PUBLIC_GA_ID=G-357NGNS717`, sur Vercel Production) **charge (par `<script>` gtag.js DOM natif, cf. correctifs ci-dessous) UNIQUEMENT apres consentement explicite** : Consent Mode v2, defaut `analytics_storage:denied` pose beforeInteractive au layout (script inline, AUCUNE requete) ; GA monte seulement sur « Accepter ». Banniere MAISON aux tokens du site (`site/src/components/Consentement.tsx`), sobre en bas d'ecran, « Accepter »/« Refuser » au MEME niveau (CNIL), choix memorise `localStorage['alm-consent']` **13 mois** (horodate -> re-demande) + re-modifiable via « **Gerer les cookies** » dans la barre legale du footer (`CustomEvent("alm:cookies")`, meme pattern que la lettre d'info). Refus = zero cookie GA, site intact. **Vercel Analytics RESTE** (cookieless, coexiste, non gate). 5 evenements de conversion (client, snake_case, ZERO PII, `site/src/lib/analytics.ts::trackEvent` envoie si consentement) : `precommande_envoyee` (param canal), `devis_envoye`, `newsletter_inscription`, `compte_cree` (garde anti-double ; = inscription soumise, en attente de confirmation email), `recommande_1_geste` (param canal) ; noms IDENTIQUES a ceux crees dans GA4 par Arnaud. Section « Cookies et mesure d'audience » ajoutee aux mentions legales (GA4 + 13 mois + re-modifiable). **SEUL changement visuel = la banniere + le lien footer** (refonte /histoire+footer inchangee). Build + lint verts ; SSR verifie : defaut denied present, AUCUN gtag.js avant consentement, « Gerer les cookies » sur toutes les pages. Branche `site-ga4-consent` supprimee apres merge. **Correctifs post-livraison (2026-07-21, EN PRODUCTION, branches supprimees)** : (1) ORDRE - le consent update granted est pousse dans `dataLayer` AVANT le `config` (sinon le page_view part en denied) [`e73ef5f`] ; (2) EXECUTION - `gtag.js` charge par un `<script>` DOM NATIF (`document.head.appendChild`), car `@next/third-parties`/`next/script` sur montage conditionnel POST-consentement emettait le preload (le fetch) mais n'executait pas le loader ; `@next/third-parties` RETIRE, `trackEvent` bascule sur `window.gtag('event',...)` [`c402b1a`]. **NB DEBUG** : le « rien dans GA4 » constate multi-appareils etait un FILTRE DNS LOCAL (`nslookup googletagmanager.com` = `0.0.0.0`) sur le Mac + tel du proprietaire, PAS le site ; les 2 correctifs restent de vrais bugs elimines. Lecons (code + debug) gravees dans `docs/site/ARCHITECTURE.md`.
  - **Bloc « Panier frais » (teasing) sur /boutique : EN PRODUCTION** (merge `--no-ff` `415ab24`, 2026-07-21 ; **le webhook Vercel du merge a manque** [0 status, 0 deployment GitHub sur le sha] -> re-trigger `e4694c9` par touche des DEUX perimetres [1 commentaire dans `site/` + 1 dans `src/` Atelier, cf. memoire] ; **les 2 projets Vercel READY** ; live verifie : `/panier-frais/confirmer` 200, **`/boutique` prod STRICTEMENT INCHANGEE** [flag OFF -> 0 bloc rendu, « Nos recettes signatures » intacte] ; validation visuelle 390/1440 par Arnaud OK avant merge). Bloc TEASING qui collecte des INTENTIONS (email + vote FACULTATIF taille/rythme/contenu) en double opt-in STRICT, pilote par un flag config-source ; il ne vend rien (V2 = vrai parcours de reservation, hors perimetre). **Flag remis OFF avant le merge** (un test Arnaud l'avait laisse ON) ; **1 intention de test** (`arnaud.depouilly@gmail.com`, confirmee) a nettoyer au pre-ouverture (ciblee par email, jamais de sweep). **Migration 0043 DEJA APPLIQUEE en prod** (comme les vagues precedentes, la base precede le merge) : table autonome `panier_frais_intention` (email + votes nullables + token/statut/consentement_le) + singleton `parametre_site` (flag `panier_frais_teasing_actif` **OFF PAR DEFAUT**) + RPC `web_intention_panier_frais` (upsert idempotent, votes coalesce) / `web_confirmer_panier_frais` (consentement horodate) ; RLS `est_chef()`, EXECUTE reserve a `site_ecrivain`, `site_lecteur` lit le FLAG jamais les intentions (prouve au niveau des roles). Site : `PanierFrais.tsx` conforme maquette, gate par `panierFraisTeasingActif()` -> **flag OFF = /boutique STRICTEMENT identique** ; route `/api/panier-frais` + page `/panier-frais/confirmer` + email `emailPanierFraisConfirmer` (mirroir newsletter). Atelier : `PanierFraisReglages` en Reglages (toggle + agregat des votes CONFIRMES uniquement, en attente affiches a part). GA4 : event `panier_frais_interet` au submit (consenti, zero PII). Detail + conformite maquette desktop/mobile : `docs/site/CONFORMITE_PANIER_FRAIS.md` ; handoff `docs/handoffs/boutique-panier-frais-*`. **DEPENDANCE D'ACTIVATION EXPLICITE : n'ACTIVER le flag en prod qu'APRES `RESEND_PROD=1`** (domaine `aleonmange.app` verifie DNS, cf. backlog pre-ouverture) — sinon les emails de confirmation partent `[DEV]` vers `RESEND_DEST_TEST` et AUCUNE intention ne peut etre confirmee (donc aucun comptage de demande valide).
  - **Emails transactionnels (Resend) : bascule PROD + gabarit de marque EN PRODUCTION** (merge `--no-ff` `131bffb` « bascule PROD + gabarit + accentuation » + `912248c` « correction nom de marque », 2026-07-21 ; webhook OK, 2 projets Vercel success ; **teste en reel BOUT-EN-BOUT par Arnaud** : precommande site -> email de marque en PROD [sans `[DEV]`, from `contact@aleonmange.app`, **reply-to `aleonmange@yahoo.com` confirme**] -> confirmation chef dans l'Atelier -> 2e email de marque -> reponse arrivant bien dans la boite yahoo). **Gabarit email-safe COMMUN** (tables + CSS inline, 600px, polices systeme, `color-scheme light` dark-mode-safe) : bandeau creme + logo `logo-alm.png` (transcode PNG du webp, heberge, HORS piece jointe) + filet rouge + corps canard + pied adresse/tel/`aleonmange@yahoo.com`/site. Applique aux **6 emails des 2 apps** (site `site/src/lib/email.ts` : precommande, devis, newsletter, panier frais ; Atelier `src/lib/email.ts` : commande confirmee, refusee) -- 2 modules DUPLIQUES (apps isolees, pas d'import partage). **Reply-to** = `aleonmange@yahoo.com` (site : `COORDONNEES.email` source unique ; Atelier : constante). **Expediteur** `contact@aleonmange.app` inchange. **Mode dev conserve** en local (`RESEND_PROD` absent -> `[DEV]` vers `RESEND_DEST_TEST`) ; **`RESEND_PROD=1` pose par Arnaud sur les 2 projets Vercel + `RESEND_API_KEY` sur l'Atelier** (Production). **Texte VISIBLE integralement accentue** (corps + titres h1 + subjects ; le nom de marque reste « A Léon Mange » plein, jamais « À »). Refactor `rendu*`/`email*` (rendu PUR = apercu sans envoi). Apercus committes : `docs/handoffs/apercus-emails/*.html` (autonomes, logo en data-URI ; fidelite prouvee par cross-check byte-identique). **REGLE PERMANENTE gravee ce chantier : PREUVE DE PUSH PAR HASH DISTANT** — apres chaque `git push`, coller `git ls-remote --heads origin <branche>` (hash distant == HEAD local), jamais se fier a une affirmation de push (un plan/agent peut DECRIRE une branche/commit inexistants). Cf. memoire [[git-preuve-push-hash-distant]].
  - **Dashboard** : rendre toutes les cartes KPI actionnables (CA du jour -> Finances, Ventes du jour -> Historique, Plats qui performent -> Ventes & tendances...) : polish UX, session dediee.
  - **Donnees de demo : INTACTES en prod** (env de test de valeur ; le go-live du 2026-07-20 est un deploiement, PAS un nettoyage, decision Arnaud). Le nettoyage cible = backlog PRE-OUVERTURE ci-dessus.
  - **create-or-match FAIT (0038)** : email prioritaire strict ; le tel ne rattache que si l'email existant est vide/identique (sinon NOUVEAU client, tel nul, index unique preserve), plus d'ecrasement. Prouve (4 scenarios). Doublons volontaires -> espace client authentifie ou chef en fiche client. Documente : ARCHITECTURE.md section create-or-match.
  - **Emails AFFICHES du site = `aleonmange@yahoo.com`** (adresse REELLE du camion, bascule 2026-07-20 ; source unique `COORDONNEES.email` -> footer, page contact, mentions legales, boutique). Backlog : **quand les chefs changent l'email du camion -> re-basculer l'email affiche du site** vers la nouvelle adresse. NB : l'EXPEDITEUR transactionnel `contact@aleonmange.app` reste ; le reply-to est passe a `aleonmange@yahoo.com` (chantier emails du 2026-07-21, cf. entree « Emails transactionnels » ci-dessus). FAIT (2026-07-20) : le mailto « suppression de compte » (`site/src/app/compte/profil/ProfilForm.tsx`) bascule aussi vers `aleonmange@yahoo.com` (toute demande entrante doit arriver dans une boite relevee). **NB `site/public/llms.txt`** (fichier GEO pour les assistants IA) contient l'email ET l'adresse de `COORDONNEES` en **COPIE STATIQUE** (il ne lit pas la source au build) -> a RESYNCHRONISER a la main a chaque maj de `COORDONNEES` (email au changement de camion, adresse, etc.).
  - **FAIT (2026-07-21)** : domaine `aleonmange.app` verifie chez Resend, `RESEND_PROD=1` pose sur les 2 projets Vercel + `RESEND_API_KEY` sur l'Atelier -> emails transactionnels en PROD (cf. entree « Emails transactionnels » ci-dessus).
  - favicon variante 16px (polish).
