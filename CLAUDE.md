@AGENTS.md

# Carte des documents du repo

- `docs/PLAN.md`, `docs/HANDOFF_CC_LOCAL.md`, `docs/AUDIT_CONFORMITE.md` : l'ATELIER (plan, reprise, conformite maquette-code ecran par ecran).
- `handoff/` : handoff maquette Atelier (reference, jamais edite).
- `docs/handoff-site/` : handoff du SITE PUBLIC (relais, INTEGRATION.md V2, maquettes desktop/mobile autonomes, plan d'integration — jamais edites).
- `docs/site/ARCHITECTURE.md` : architecture du site public (monorepo, acces donnees service_role, plan de migration referentiel, risques Vagues 2-3).
- `docs/site/CONFORMITE_VAGUE_1.md` : conformite maquette-code du site, Vague 1.
- `site/` : app Next.js du site public (port 3002, README env : `site/.env.local.example`).

# Etat du chantier site public (a tenir a jour a chaque fin de session)

- Vague en cours : **VAGUE 4** sur `site-vague-4` (socle DB 0034-0040 + infra auth + **PHASE B = 4 ecrans espace client + fiche client Atelier**, LIVRES/buildes/prouves ; reste = validation visuelle 390/1440 par Arnaud + STOP final avant merge de la vague). Vagues 1-2-3 EN PRODUCTION sur `main`. **HOTFIX SECURITE deja EN PROD sur `main` (commit `1ba235a`, `--no-ff` scope hotfix)** : garde Atelier exige `app_role=equipe` (correctif faille constat C = un compte client atteignait l'UI Atelier, donnees protegees par la RLS mais shell expose) ; l'Atelier a rebuilde, le site a saute. Le RESTE de la Vague 4 (site-vague-4) n'est PAS merge (aucun deploiement site declenche).
- VAGUE 1 (vitrine lecture) EN PRODUCTION sur **https://aleonmange.app** depuis le 2026-07-19 (www → apex 308 ; Atelier sur **https://atelier.aleonmange.app**) ; role `site_lecteur` (JWT echeance 2036-07-18). Plan de migration referentiel 0019-0023 APPLIQUE le 2026-07-18. Etat live, rollback, pieges Vercel : `docs/site/ARCHITECTURE.md`. Conformite : `docs/site/CONFORMITE_VAGUE_1.md`.
- VAGUES 2 ET 3 EN PRODUCTION depuis le 2026-07-19 (MERGE `--no-ff` `a48b497` « Merge Vagues 2 et 3 », les DEUX projets Vercel rebuildes). Le site prend des commandes (click&collect boutique + truck, devis traiteur, newsletter double opt-in) et le chef les confirme/refuse et les traite dans l'Atelier.
  - VAGUE 2 (les trois ecritures) : migrations 0024-0030. Ecritures via role `site_ecrivain` (JWT echeance 2036-07-19 ; AUCUN droit table, EXECUTE sur 4 RPC SECURITY DEFINER only) ; prix recalcule en SQL ; create-or-match (email prioritaire, telephone secours) ; emails Resend best-effort (mode dev). Details : `docs/site/CONFORMITE_VAGUE_2.md`, mecanisme : ARCHITECTURE.md section Vague 2.
  - VAGUE 3 (confirmation chef) : migrations 0031-0032. Confirmation (depliage bowl via source unique `composerLignesComposantBowl`), refus (marqueur, REGLE PERMANENTE : tout lecteur de `web_a_confirmer` filtre `refuse_le`), remise B2C (moyen corrige au retrait), badge + section /orders + notification (trigger defensif), editeur `creneau_retrait`, emails Atelier (module propre). Details : `docs/site/CONFORMITE_VAGUE_3.md` + ARCHITECTURE.md section Vague 3.
- GO-LIVE TESTE DE BOUT EN BOUT EN PRODUCTION le 2026-07-19 : 2 vraies commandes truck sur aleonmange.app → notification (trigger) + pastille + section chef → CONFIRMATION chef (depliage bowl : composants conformes recette, entree dans `v_commande_ouverte`) → REMISE chef (moyen especes, `reglement` + `encaisse_le` poses, entree en CA facture ET encaisse). Cycle complet prouve en base. Donnees d'essai NETTOYEES (comptages a zero verifies : 0 vente web, 0 client test, 0 notification `commande`, 0 orphelin).
- HOTFIXES SITE LIVRES le 2026-07-19 (branche `hotfix-precommande`, merge `--no-ff`, EN PRODUCTION sur le site ; seul le projet SITE a rebuilde, l'Atelier a saute = nouvelle preuve de l'Ignored Build Step) :
  1. `/food-truck/precommander` — date de retrait COMPLETE (jour + date, ex. « Mardi 21 juillet ») cote client dans le selecteur d'emplacement ET un recap, via `prochainRetraitTruck` (`site/src/lib/data/creneaux.ts`) cable dans `precommander/page.tsx` : MEME source que le `due_at` pose a la commande (`/api/commande`) -> affichage coherent avec le calcul Europe/Paris. Les emails portaient DEJA la date complete (site « precommande recue » = `retraitLabel` ; Atelier « confirmee » = `labelRetrait`).
  2. `BoutonSubmit` (`site/src/components/forms.tsx`) : `className` passee est desormais destructuree hors des props et FUSIONNEE dans les classes de base, au lieu d'etre ecrasee par le spread `{...props}` -> le CTA « Envoyer ma demande » retrouve le style pilule accent (boutique + truck). E2E : classe fusionnee verifiee dans le HTML SSR des deux parcours, commande d'essai truck -> retrait affiche « Mardi 21 juillet » == `due_at` en base (2026-07-21 11:30 Europe/Paris, isodow 2), builds verts des deux apps, donnees d'essai nettoyees.
- IMAGES SITE (visuels du handoff CD) EN PRODUCTION depuis le 2026-07-19 (merge `--no-ff` `3cb18b2` ; seul le projet SITE a rebuilde, l'Atelier a SAUTE = Ignored Build Step, prouve : deploiement Atelier CANCELED). Verif prod : images rendues sur les 5 pages, portraits chefs en placeholder, poids webp 79-165 Ko (next/image sert ~67 Ko). **AMENDEMENT REGLE (decision Arnaud) :** la regle « aucune image generee par IA » visait les FAUSSES representations non validees, PAS les visuels du handoff CD (direction artistique validee) ; ces visuels sont integres et **restent remplacables par un shooting reel plus tard SANS changement de code** (memes emplacements, memes noms de fichiers). Controle anti-Foodizy MAINTENU (RAS sur les 22 visuels extraits des maquettes). Detail : `docs/site/ARCHITECTURE.md` section Visuels.
  - Ambiance -> composant `Photo` (next/image, webp `site/public/images/`, alt FR sans cadratin, priority sur les hero) : accueil (hero « la maison » + 3 cartes canaux), boutique (comptoir), food-truck (le truck + au marche), traiteur (hero + galerie 4 receptions), histoire (laboratoire).
  - **PORTRAITS DES CHEFS** (« Audrey et Victorien » : accueil + histoire) : `PhotoAvenir` MAINTENU, decision definitive de cette vague (jamais de visage genere presente comme un vrai chef ; ce sera la 1re photo du shooting reel).
  - Plats par la DONNEE : migration **0033** `produit.image_url` (additif, faible risque, applique + verifie). 3 produits boutique pourvus (Parmentier de boeuf, Cordon bleu, Lasagnes bolognaise) -> section « Nos recettes signatures » pilotee par le catalogue (fallback etat vide propre si aucune image).
  - Ecartes : logo fond noir (detourage sale), image « plan d'acces » (figee, mail @yahoo d'artefact) -> a la place le lien « Voir le plan d'acces » ouvre Google Maps ITINERAIRE sur l'adresse reelle (`COORDONNEES.plan`).
  - EN ATTENTE (a trancher/associer, listes au chef) : **bowl + tartines** (signatures truck) et **blanquette + paella** (aucun produit correspondant au catalogue) non poses ; upload d'images produit par les chefs dans l'Atelier (Vague 4) ; demander a CD un **logo vectoriel fond transparent** pour l'en-tete.
- VAGUE 4 — **SOCLE DB + INFRA AUTH FAITS, PROUVES, COMMITTES** sur `site-vague-4` (migrations **0034-0040 APPLIQUEES EN PROD** ; hook Auth ACTIVE ; etat final verifie le 2026-07-19 : trigger `handle_new_user` FAIL-CLOSED + signups ON). Inclut le correctif securite **0040** (faille self-provision chef fermee) et le write-path client **0039** (RPC rattachement email verifie + opt-in + maj profil). **Branche POUSSEE (`git push`), NON MERGEE** ; **main = Vagues 1-3 + hotfix + images** (aucun deploiement declenche). Plan : `~/.claude/plans/delegated-tumbling-pearl.md`. Detail + preuves : `docs/site/CONFORMITE_VAGUE_4.md` ; modele de securite : `docs/site/ARCHITECTURE.md` section Vague 4.
  - Migrations : 0034 (hook claim `app_role` + `est_chef()` fail-closed ; son trigger `handle_new_user` **remplace par 0040**), 0035 (bascule 60 policies `using(true)` -> `using(est_chef())` + 5 scoped durcies), 0036 (isolation client : `client.auth_user_id`, `mon_client_id()`, policies client SELECT, `client_preference`), 0037 (fidelite : opt-in, `parametre_fidelite`, `fidelite_redemption`, vue derivee `v_fidelite_client`), 0038 (create-or-match email prioritaire strict), **0039** (write-path client : RPC `web_rattacher_compte_client` [email VERIFIE, param-free] + `web_maj_profil_client` [opt-in date + profil], SECURITY DEFINER self-scope, EXECUTE authenticated only, garde est_chef), **0040** (`handle_new_user` FAIL-CLOSED : profil pour le bootstrap owner uniquement ; ferme la faille self-provision chef exposee par l'activation des signups).
  - **SOCLE FAIT (Etage 2a)** : infra auth `@supabase/ssr` server-only (aucun nouveau secret, jamais NEXT_PUBLIC), `proxy.ts` cadre STRICTEMENT `/compte` (vitrine publique intacte), Server Actions inscription `kind='client'` / connexion / deconnexion, callback confirmation (PKCE `?code=` + `verifyOtp ?token_hash=`) -> rattachement 0039 + opt-in, ecrans SOCLE sobres (connexion + /compte) ; Atelier `/login` action `signUp` supprimee. **Build + lint verts des 2 apps.** **E2E socle A VALIDER par Arnaud** (inscription -> mail -> lien -> rattachement -> isolation) AVANT Phase B.
  - **PHASE B FAITE** (buildee + prouvee, non mergee) : (a) 4 ecrans maquette CD (d-login, d-compte carte fidelite tampons derives + chips + statut verrouille, d-cmd + re-commande 1-geste [recompose le panier -> `/api/commande` web_a_confirmer, create-or-match email], d-profil coordonnees/consentements/preferences) + entree « Mon compte » en-tete ; (b) Atelier fiche client `/clients` : palier `v_fidelite_client` + geste « appliquer recompense » (`fidelite_redemption`, chef) ; (c) E2E data-path prouve (compteur derive, re-commande attachee/chef-gated, opt-in non retroactif, isolation) + **tableau de conformite ecran par ecran** dans `CONFORMITE_VAGUE_4.md`. Ecarts volontaires signales (CP lecture seule, newsletter = action S'abonner, suppression compte = demande email, zero cadratin).
  - **Constats de la validation socle (Arnaud)** : socle valide (inscription -> mail -> lien -> rattachement -> isolation OK). Deux correctifs livres : **Fix B** (site_url -> aleonmange.app + emailRedirectTo exact : les confirmations client ne partent plus vers l'Atelier) ; **Fix C / constat C** (garde Atelier exigeait seulement « connecte », pas le claim -> un compte client atteignait l'UI Atelier ; corrige : middleware + signIn + layout exigent `app_role=equipe`). Fix C **HOTFIXE en prod (main, `1ba235a`)** ; Fix B sur site-vague-4.
  - **RESTE = validation visuelle + STOP final** : (1) Arnaud valide le rendu 390/1440 des 4 ecrans + le parcours re-commande live (compte reel) ; (2) **STOP final** : merge `--no-ff` de la vague sur feu vert (go-live = son propre evenement) + nettoyage des donnees de demo (dont les precommandes en retard). Config Auth deja posee (site_url + redirect URLs `/compte/**` dev+prod).
  - Backlog technique (a traiter pendant le code) :
  - **/users : cabler « Inviter un utilisateur »** sur l'appartenance equipe (= table `profil`, source de verite du hook) : ajout email -> insert `profil` -> claim `app_role=equipe` au prochain jeton. **Depuis 0040 (fail-closed), le trigger ne provisionne PLUS AUCUNE equipe automatiquement (owner-bootstrap only)** : `/users` (ou un insert `profil` SQL cible) est le SEUL chemin pour un nouveau membre d'equipe. TRES PRIORITAIRE : conditionne l'arrivee d'Audrey et Victorien (comptes equipe a creer par insert profil).
  - **Atelier `/login`** : **FAIT (2026-07-19, commit `9d79503`)** — action `signUp` non cablee supprimee (elle aurait provisionne un chef, cf. faille 0040) + commentaires des 2 fichiers corriges. L'UI n'invitait deja pas a creer un compte.
  - **Dashboard** : rendre toutes les cartes KPI actionnables (CA du jour -> Finances, Ventes du jour -> Historique, Plats qui performent -> Ventes & tendances...) : polish UX, session dediee.
  - **Nettoyage des donnees de demo** avant ouverture reelle (dont les **5 precommandes en retard du 13-18 juillet**) : cible par id/email, JAMAIS de sweep.
  - **create-or-match FAIT (0038)** : email prioritaire strict ; le tel ne rattache que si l'email existant est vide/identique (sinon NOUVEAU client, tel nul, index unique preserve), plus d'ecrasement. Prouve (4 scenarios). Doublons volontaires -> espace client authentifie ou chef en fiche client. Documente : ARCHITECTURE.md section create-or-match.
  - verification domaine `aleonmange.app` chez Resend (DNS zone Vercel) pour sortir les emails du mode dev (`RESEND_PROD=1`) : je fournis les enregistrements DNS.
  - favicon variante 16px (polish).
